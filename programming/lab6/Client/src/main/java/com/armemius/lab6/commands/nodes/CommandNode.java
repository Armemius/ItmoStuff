package com.armemius.lab6.commands.nodes;

/**
 * Node that represents one of the command's path variations
 */
public class CommandNode extends Node {
    public CommandNode(String name) {
        super(name);
    }
}
